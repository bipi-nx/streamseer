# Twitch app client IDs — public by design (implicit OAuth has no secret).
# A Twitch app only accepts redirect URIs registered against it, matched
# byte-for-byte, so dev and prod use separate apps.
#   DEV  app must list: http://localhost:5173
#   PROD app must list: https://streamseer.vercel.app
VITE_TWITCH_CLIENT_ID=52vrmex00arz9c8tywsoynlrhbzntq
VITE_TWITCH_CLIENT_ID_PROD=xaes7krcadtowhu2vhke4fgt0ziheg
